# Changelog

## 2.1.1

- 恢复 1.1.4 默认速度显示：直接显示 aria2 `downloadSpeed`，连接中隐藏瞬时值，暂停时不伪造 0 B/s
- 不再把速度显示改成浏览器写盘增量或自行平滑

## 2.1.0

- 下载机制回到 1.x 的 aria2 + Native Messaging，速度读取 aria2 统计
- 保留新版面板、64 路选项、即时反馈、横排按钮与 8 秒圆环倒计时
- 恢复本机文件夹选择与路径设置、续传重试、临时分片清理；安装包重新包含安装脚本
- 纯浏览器实现归档至 legacy/browser-2.x，切换前需结束原有任务

## 2.0.1

- 文件夹提醒缩小并将按钮横排，显示 8 秒圆环倒计时，到时自动关闭
- 文件夹状态检查失败、无响应或网页残留旧扩展脚本时，显示恢复提示，不再静默留下“等待网站响应”
- 文件夹检查与设置入口不等待旧任务恢复，避免被无关初始化阻塞
- 转交 Chrome 下载时显示原因，扩展面板底部显示版本号

## 2.0.0 补充更新

- 未设置或未授权下载文件夹时，面板持续提醒；网页下载提示提供选择文件夹入口，不再自动消失
- 恢复 64 路选项，并同步后台设置校验和下载引擎并发上限

## 工程结构整理

- 2.0.0 主源码由 standalone 移至 extension，使用说明归入 docs
- 1.x 原生版源码、测试、安装脚本和组件许可归档至 legacy/native-1.x
- 增加根目录开发命令、scripts 打包工具和 GitHub Actions 发布包产物
- 生成文件统一放入 dist，测试浏览器与临时数据继续放入 work；均不提交 Git

## 2.0.0 纯扩展版（独立的 standalone 目录）

- 无 CMD、Python、Native Messaging 或 aria2，Chrome 开发者模式加载即可使用
- 首次授权目标目录，Offscreen Worker 用 Range GET 分段并直接流式写盘，4/8/16/32 路
- 无 HEAD 前置请求；普通文件链接即时点击提示，浏览器确认后显示已接收
- 强 ETag 一致性检查、暂停继续、部分连接重试、单连接降级与取消清理
- 独立扩展 ID，不替换已有原生安装；完整边界和验证方式见 standalone/README.md

## 1.1.4 原生版

- 接管任务在本机引擎握手之前显示，避免点击后空白等待
- 面板使用缓存引擎状态并异步刷新，不等待 ping 和状态轮询
- 任务存储变化立即刷新面板；普通文件链接点击时显示非阻塞反馈
- 不再在每次任务状态写入时插入历史清理 RPC，保留空闲和终态清理

## 1.1.3

- 下载完成、取消或最终切回 Chrome 后自动清理该任务的临时文件与续传文件
- 最后一个任务清理后删除空的 `.ChromeParallelDownload` 目录
- 保留活动、暂停、自动重试及尚未交付的任务；文件占用时保留清理信息并稍后重试
- 清理只针对校验后的任务目录和明确文件名，不递归删除，不跟随链接或目录联接

## 1.1.2

- 正确解析 aria2 通过 HTTP 400 返回的 JSON-RPC 错误，修复失败任务无法取消或切回 Chrome 的问题
- 服务器提前断开、超时和已识别的临时 TLS 握手失败时，保留分片并自动断点重试，最多 3 次
- 首次重试保留并发数，反复失败时再减少并发；证书验证保持开启
- 重试提示与已下载字节数同时显示；修复失效任务元数据清理
- 移除 aria2 不识别的 max-redirect 参数

## 1.1.1

- 修复本机 RPC 短暂超时就重复启动引擎的问题
- 启动恢复检查限制在约 7 秒内，避免超过扩展的 15 秒消息超时
- 增加跨桥接进程的启动互斥锁；端口已占用时不再启动第二个引擎
- 启动失败时清理本次产生的进程，保留已有引擎和下载任务

## 1.1.0

- 支持系统文件夹选择器和手动输入下载目录，新设置不改变已有任务的目的目录
- 支持跟随系统、浅色和深色外观，保存主题偏好
- 面板使用单一页面滚动，保留仅展示当前任务的行为
- 安装脚本支持更新下载路径、中文路径，以及重复安装时复用已校验的引擎
- 整理源码、运行文件忽略规则、分享说明和自动化测试

## 1.0.3

- 仅显示正在接管、下载、排队或暂停的任务
- 完成、取消和切回 Chrome 后自动清理任务记录
- 清理元数据时保留下载文件和临时分片

## 1.0.2

- 取消自动弹出扩展面板
- 增加 32 路和 64 路连接选项

## 1.0.1

- 增加 Chrome 暂停任务说明
- 改进接管和切回 Chrome 的状态提示

## 1.0.0

- 首次发布
- 自动接管、暂停、继续、取消和失败回退
- 4、8、16 路并发连接
